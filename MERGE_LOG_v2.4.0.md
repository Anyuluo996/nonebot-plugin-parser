# 上游 v2.4.0 合并日志

**合并日期**: 2026-02-14
**上游版本**: v2.4.0 (从 v2.3.8)
**合并分支**: master

---

## 一、合并概述

本次合并选择性地整合了上游 v2.4.0 的有用功能，同时保留了我们的核心特性。

---

## 二、已合并的内容

### 2.1 依赖更新 ✅

| 依赖 | 变更 | 说明 |
|------|------|------|
| `pillow` | → 12.1.1 | 安全更新 |
| `tqdm` | 4.67.1 → 4.67.3 | 小版本更新 |
| `nonebot-plugin-alconna` | 0.59.4 → 0.60.4 | 功能更新 |
| `ruff` | - | 添加 `ASYNC240` 到忽略列表 |

**提交**: `merge: 合并上游依赖更新`

---

### 2.2 NGA 解析模块 ✅

采用了**混合方案**，结合双方优点：

#### 上游引入的功能:
- ✨ **图文混排** - 使用 `create_graphics_content()` 将图片与描述文本绑定
- 📝 更好的日志调试信息 (`logger.debug`)
- 兼容 `build_url_by_tid()` 方法

#### 保留的我们的功能:
- 🛡️ **完整的 BBCode 清理** - `clean_nga_text()` 函数
  - 处理 `[url]`、`[quote]`、`[color]`、`[size]` 等标签
  - 处理不完整标签
  - 空白字符清理
- ⬇️ NGA 特殊下载支持 (在 download 模块中)

**提交**: `feat(nga): 混合合并上游NGA图文混排功能`

---

### 2.3 渲染器资源文件 ✅

获取了上游的静态资源文件：
- 🖼️ `avatar.png` - 静态头像，可替代代码绘制的占位符
- ▶️ `play.png` - 播放按钮图标

这些资源已添加，但代码集成留待后续进行。

**提交**: `feat(render): 添加上游静态资源文件`

---

### 2.4 文档更新 ✅

- README 添加上游的 star history 图表

---

## 三、未合并的内容 (保留我们的实现)

### 3.1 匹配器模块
- ❌ 强制解析前缀功能 (`PARSER_FORCE_PREFIX`)
- ❌ 群级别平台禁用功能
- ❌ 细致的表情反应处理

### 3.2 下载器模块
- ❌ NGA curl_cffi 特殊下载处理
- ❌ URL 级别并发下载锁
- ❌ tqdm 进度条 (上游改为 rich)

### 3.3 渲染器模块 (代码部分)
- ❌ GIF 转换功能 (仅推特)
- ❌ `suppress_exception_async` 装饰器
- ❌ 细致的图片网格处理逻辑
- ❌ 头像占位符绘制代码 (有静态资源可替代)

### 3.4 配置模块
- ❌ `parser_force_prefix` 配置项

---

## 四、新增的文档

本次合并创建了以下分析文档：

| 文档 | 说明 |
|------|------|
| `UPSTREAM_MERGE_PLAN_v2.4.0.md` | 整体合并计划 |
| `NGA_PARSER_COMPARISON.md` | NGA 模块详细对比 |
| `RENDERER_COMPARISON.md` | 渲染器模块详细对比 |

---

## 五、后续建议

### 5.1 短期可继续合并
- [ ] 渲染器：使用静态头像替代代码绘制
- [ ] 渲染器：合并且高度估算修复 (`58b4019`, `9701b06`)
- [ ] 渲染器：代码简化清理

### 5.2 长期考虑
- [ ] 评估是否将进度条从 tqdm 迁移到 rich
- [ ] 评估是否简化异常处理装饰器
- [ ] 定期关注上游更新

---

## 六、测试建议

合并后建议测试以下功能：
1. NGA 链接解析 - 确认图文混排正常
2. B站 链接解析 - 确认我们的修复仍然有效
3. 推特链接解析 - 确认 GIF 转换功能正常
4. 强制解析前缀 - 确认功能正常
5. 平台禁用 - 确认群级别禁用正常

---

## 七、总结

本次合并成功整合了上游 v2.4.0 的核心改进：
- ✅ 依赖安全更新
- ✅ NGA 图文混排展示
- ✅ 静态资源文件

同时完整保留了我们的核心竞争力：
- ✅ 强制解析前缀
- ✅ 平台禁用控制
- ✅ 推特 GIF 转换
- ✅ NGA 反爬虫下载
- ✅ B站 多项修复

**合并状态**: 成功完成阶段性合并 ✅
