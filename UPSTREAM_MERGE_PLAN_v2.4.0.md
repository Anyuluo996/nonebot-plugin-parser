# 上游 v2.4.0 合并分析文档

## 概述
- 上游版本: v2.4.0 (从 v2.3.8 升级)
- 本地分支: master
- 分析日期: 2026-02-14

---

## 一、不冲突、建议直接合并的更新

### 1.1 安全和依赖更新
| 提交 | 说明 | 状态 |
|------|------|------|
| `f55e998` | Pillow 升级到 v12.1.1 (安全更新) | ✅ 已合并 |
| `e7c8998` | 升级 ruff 0.15.0 和 yt-dlp 2026.2.4 | ⚠️ 需要处理 |
| `b11d971` | 升级 yt-dlp 和 types-yt-dlp 2026.1.29 | 我们已有类似更新 |
| `584cb6d` | 放宽 rich 版本限制 | ✅ 建议合并 |
| `3b760e1` | 更新 uv_build 到 >=0.10.0,<0.11.0 | ✅ 建议合并 |
| `f3893a2` | 升级 urllib3 到 2.6.3 | ✅ 建议合并 |

### 1.2 文档和元数据更新
| 提交 | 说明 | 状态 |
|------|------|------|
| `58ff547` | README 添加 star history 图表 | ✅ 建议合并 |
| `9a10b24` | 更新插件描述和使用说明 | ✅ 建议合并 |
| `7447c60` | 回退 pre-commit 自动更新配置 | ✅ 建议合并 |
| `f0f0cd7` | pre-commit 自动更新 | 可忽略 |

### 1.3 资源文件更新
| 提交 | 说明 | 状态 |
|------|------|------|
| `c115378` | 使用静态头像替代占位符 | ✅ 建议合并 (新增资源文件) |

---

## 二、存在冲突、需要详细对比的功能

### 2.1 下载器模块 (download/__init__.py)

#### 上游改动:
- 从 `tqdm` 改为 `rich` 进度条
- 新增 `chunk_size` 可配置参数
- 新增 `download_m3u8` 方法
- 新增 `IgnoreException` 异常类型
- 重构 `streamd` → `download_file`
- **添加了 URL 级别锁防止重复下载** (`653cb23`)

#### 我们的改动:
- 添加了 NGA 图片使用 `curl_cffi` 下载的特殊处理
- **添加了 URL 级别锁防止重复下载** (`0eca83d`)
- 保留了 `tqdm` 进度条

#### 冲突点分析:
1. **并发下载保护**: 两边都实现了，但实现方式不同
   - 上游: 简单的锁机制 (但在当前看到的代码中似乎被移除了?)
   - 我们: 完整的 `_download_locks` 字典实现 + NGA 特殊处理

2. **进度条**: 上游用 `rich`, 我们用 `tqdm`

3. **方法名**: `streamd` vs `download_file`

#### 合并建议:
**保留我们的实现**，原因:
- 我们的实现包含 NGA 特殊处理，这是独有的功能
- 我们的并发锁实现更完整
- 可以考虑后续单独适配 rich 进度条

---

### 2.2 匹配器模块 (matchers/__init__.py 和 rule.py)

#### 上游改动:
- 简化了 `parser_handler`，移除了状态管理和平台检查
- `parser_handler` 现在使用 `@UniHelper.with_reaction` 装饰器
- 移除了强制解析相关代码

#### 我们的改动:
- 添加了 `PSR_FORCE_PARSE_KEY` 状态管理
- 添加了强制解析前缀功能
- 添加了平台启用状态检查 (群级别禁用)
- 添加了表情反应 (处理中/完成/失败)
- 保留了不使用装饰器的表情反应处理方式

#### 冲突点分析:
这是**核心功能冲突**，我们的功能更丰富:
1. 强制解析前缀 (`PARSER_FORCE_PREFIX`)
2. 群级别平台禁用
3. 更细致的表情反应

#### 合并建议:
**完全保留我们的实现**，上游的改动是简化，而我们增加了重要功能。

---

### 2.3 配置模块 (config.py)

#### 上游改动:
- 调整了初始化顺序 (`gconfig` 和 `_nickname` 在类定义之后)
- 移除了 `parser_force_prefix` 配置项

#### 我们的改动:
- 添加了 `parser_force_prefix: str = ""` 配置项
- 添加了 `parse_prefix` property
- 调整了初始化顺序 (确保 `_nickname` 在 `pconfig` 之前初始化)

#### 冲突点分析:
- 初始化顺序两边都做了调整，但目的不同
- 我们有独有的 `parser_force_prefix` 配置

#### 合并建议:
**保留我们的实现**，我们的配置项是必需的功能。

---

### 2.4 B站解析模块 (parsers/bilibili/__init__.py)

这是我们改动最多的模块，包含:
- Chrome 截图渲染功能 (动态和 Opus)
- 大量的 bug 修复 (转发动态、图片处理等)
- 选择器优化
- 截图裁剪优化

上游也有改动:
- `6c7b059` 增强哔哩哔哩图文动态解析

#### 合并建议:
**需要详细对比后手动合并**，保留我们的所有功能，同时检视上游的改进。

---

### 2.5 渲染器模块 (renders/common.py)

#### 上游改动:
- `c7967db` 大幅简化 common render
- `58b4019` 增强高度估算
- `9701b06` 修正文本高度估算的操作符优先级

#### 我们的改动:
- GIF 转换功能 (仅推特)
- 视频转 GIF 发送逻辑

#### 合并建议:
**需要详细对比**，上游的简化可能与我们的 GIF 功能有交互。

---

### 2.6 NGA 解析 (parsers/nga.py)

#### 上游改动:
- `f17b453` 将 NGA 信息解析为图片 (#467)

#### 我们的改动:
- NGA 下载的特殊处理 (curl_cffi)

#### 合并建议:
**可以合并**，上游的新功能值得拥有，需要检查是否与我们的下载处理兼容。

---

## 三、其他文件清理 (上游删除的)

上游删除了大量文件:
- 测试文件 (`tests/` 下的很多临时测试)
- 文档文件 (`CHANGES_SUMMARY.md`, `FEATURE_FORCE_PARSE.md` 等)
- Pipeline 输出文件

#### 合并建议:
- **保留**我们的文档 (`FEATURE_FORCE_PARSE.md`)
- 可以考虑清理上游删除的测试文件，但先备份
